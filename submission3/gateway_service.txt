Unity AI Gateway Build 3 — live reproducible configuration
Executed: 2026-08-27
Workspace profile: fe-sandbox-last-penguin
Workspace ID: 7474655051393778
UC parent: schemas/last_penguin_catalog.nimbus

MODEL SERVICE CREATE REQUEST (executed previously; live GET below proves creation)
databricks api post '/api/2.1/unity-catalog/model-services?parent=schemas%2Flast_penguin_catalog.nimbus&model_service_id=SERVICE' --json '{"config":{"routing":{"destinations":[{"name":"gpt-5-4-mini","destinationType":1,"pay_per_token_config":{"model":"models/system.ai.databricks-gpt-5-4-mini"}}]},"inference_table":{"parent":"schemas/last_penguin_catalog.nimbus","disabled":false,"table_name_prefix":"SERVICE"}}}' --profile fe-sandbox-last-penguin

Services:
- last_penguin_catalog.nimbus.nimbus_app_gateway
  inference table ID: 0cc02a9a-fbb7-4151-94a9-075f8fa811f9
  table: last_penguin_catalog.nimbus.nimbus_app_gateway_payload
- last_penguin_catalog.nimbus.nimbus_coding_agent_gateway
  inference table ID: 808159e7-8f4d-4407-bea3-4a02400bcde8
  table: last_penguin_catalog.nimbus.nimbus_coding_agent_gateway_payload

CUSTOM SERVICE POLICY FUNCTION (executed successfully)
CREATE OR REPLACE FUNCTION last_penguin_catalog.nimbus.block_unbounded_lakebase_read(event VARIANT)
RETURNS VARIANT
LANGUAGE SQL
RETURN CASE
  WHEN event:type::string = 'request' AND (
    replace(lower(event:context.message::string), '-', ' ') LIKE '%all lakebase data%' OR
    replace(lower(event:context.message::string), '-', ' ') LIKE '%unlimited read%' OR
    replace(lower(event:context.message::string), '-', ' ') LIKE '%unlimited quer%' OR
    replace(lower(event:context.message::string), '-', ' ') LIKE '%full database scan%' OR
    replace(lower(event:context.message::string), '-', ' ') LIKE '%read every table%'
  ) THEN to_variant_object(named_struct(
    'result', 'DENY',
    'reason', 'Unbounded Lakebase reads are not permitted. Request a bounded table, key, filter, and limit.'
  ))
  ELSE to_variant_object(named_struct('result', 'ALLOW', 'reason', ''))
END;

POLICY ATTACHMENT (executed in the documented Unity AI Gateway UI)
- name: nimbus-bounded-lakebase-reads
- scope: last_penguin_catalog.nimbus.nimbus_app_gateway only
- type: CUSTOM
- handler: functions/last_penguin_catalog.nimbus.block_unbounded_lakebase_read
- function_id: 1ea7348d-11e4-4c0c-9fea-f881d0a870fe
- rank: 10
- input behavior: function branches on event:type = request (ON CALL / pre_call)
- coding-agent service attachment: none

LIVE VERIFICATION COMMANDS
databricks functions get last_penguin_catalog.nimbus.block_unbounded_lakebase_read --profile fe-sandbox-last-penguin
databricks api get /api/2.1/unity-catalog/model-services/last_penguin_catalog.nimbus.nimbus_app_gateway --profile fe-sandbox-last-penguin
databricks api get /api/2.1/unity-catalog/model-services/last_penguin_catalog.nimbus.nimbus_coding_agent_gateway --profile fe-sandbox-last-penguin

LIVE BUDGET
- budget ID: eac80ca4-e2af-46e1-9788-ad39925e31d6
- name: nimbus-app-gateway-live-cap
- workspace: last-penguin (7474655051393778)
- resource type: Unity AI Gateway
- resource tag filter: application=nimbus-growth-desk
- shared thresholds: none
- default per-user block: $0.05 monthly list-price spend
- live status at final export: Setting up budget
- actual post-threshold check: HTTP 200, request ed54a642-b67b-4b07-b959-c8f91e7bb232

LIVE ALERT BUDGET
- budget ID: fec429e5-e934-4001-ba91-83e57fa16d49
- name: nimbus-app-gateway-live-alert
- workspace, resource type, and tag filter: identical to the block budget
- shared thresholds: none
- default per-user alert: $0.03 monthly list-price spend
- alert recipient: configured in the account UI and redacted from evidence
- live status at final export: Setting up budget

The 2026-08-27 Beta UI permits one default per-user threshold per budget, so
the alert and block use two identical-scope budgets. No shared block exists.

Deployment route: /ai-gateway/mlflow/v1/chat/completions
The Build 3 app does not use the legacy /serving-endpoints route.

IMPORTANT LIVE SEMANTICS
The current Service Policies Beta returns HTTP 200 for a DENY decision, with
id=databricks-guardrail-block, finish_reason=content_filter, zero tokens, and a
top-level databricks_service_policy object. The app converts this structured
upstream denial to a client-facing 403 without performing local prompt matching.
